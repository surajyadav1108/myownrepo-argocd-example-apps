apiVersion: v1
kind: ConfigMap
metadata:
  name: example-configmap
  annotations:
    example.com/description: "This is an example ConfigMap with annotations exceeding the limit. " +
                             "This annotation content is artificially long to demonstrate the issue. " +
                             "Please ensure that your annotations do not exceed the allowed limit."
    example.com/long-annotation: |
      This is a long annotation content that exceeds the allowed limit in Kubernetes. 
      It is being created for testing purposes to simulate a scenario where annotations are too large. 
      Please review and optimize your annotations to ensure they fit within the specified constraints.
data:
  key1: value1
  key2: value2
